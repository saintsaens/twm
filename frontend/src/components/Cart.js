import React from 'react';

function Cart() {
  const totalItems = 0;
  const totalPrice = 0;

  return (
    <div>
      <p>Items in cart: {totalItems}</p>
      <p>Total price: {totalPrice}</p>
    </div>
  );
}

export default Cart;
